'use babel';

import KeybindingsManagerView from '../lib/keybindings-manager-view';

describe('KeybindingsManagerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
